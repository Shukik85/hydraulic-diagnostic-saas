# Modbus TCP/RTU Protocol Support
# Required for industrial sensor data collection

# Core Modbus protocol library
pymodbus>=3.11.0,<4.0.0

# Serial communication for Modbus RTU
pyserial>=3.5

# Async HTTP client for HTTP JSON protocol fallback
aiohttp>=3.9.0

# Structured logging for protocol operations
structlog>=24.0.0

# Production monitoring and metrics
prometheus-client>=0.19.0
