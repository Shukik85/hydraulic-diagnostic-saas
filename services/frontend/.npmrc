# Enable package-lock for reproducible builds
package-lock=true

# Audit level
audit-level=moderate

# Peer dependencies
strict-peer-dependencies=true
legacy-peer-deps=false

# Save exact versions
save-exact=false
save-prefix=^

# Progress
progress=true
loglevel=warn
