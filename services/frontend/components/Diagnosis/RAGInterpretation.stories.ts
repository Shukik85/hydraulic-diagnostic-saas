// Storybook: импорт mock и типов из относительного пути, не используя ~
import RAGInterpretation from './RAGInterpretation.vue'
import { diagnosisMocks } from '../../../mocks/diagnosis-result.mock'

export default { /* не меняется */ }

// аналогично для других stories фрагментов
