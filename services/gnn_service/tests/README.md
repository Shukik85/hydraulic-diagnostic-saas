# tests/README.md

Новая структура тестов:

- `tests/unit/` — unit-тесты ядра inference
- `tests/integration/` — интеграционные тесты движка
- `tests/mocks/` — общие моки/фикстуры
- `tests/conftest.py` — глобальные фикстуры
