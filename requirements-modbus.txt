# Modbus TCP/RTU Development Requirements
# Working versions tested with production code

# Modbus protocol support
pymodbus>=3.11.0,<4.0.0

# Async HTTP client (for HTTP JSON protocol)
aiohttp>=3.9.0

# Structured logging
structlog>=24.0.0

# Serial communication (for Modbus RTU)
pyserial>=3.5

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional: OPC UA support (if needed later)
# asyncua>=1.0.0

# Production monitoring
prometheus-client>=0.19.0
